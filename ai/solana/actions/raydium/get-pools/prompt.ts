export const SOLANA_GET_POOLS_PROMPT = `This function gets all of the liquidity pools for a token.
The function requires a symbol, or a mint address.
If a user asks for the native token of the BigBased, this platform, call this function with the contract address of the native token: <currently-unavailable>.`;
